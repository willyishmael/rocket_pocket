enum LoanStatus { ongoing, completed, overdue, cancelled }

enum LoanType { given, taken }

enum LoanFinancingKind { cashLoan, purchaseInstallment }

enum LoanInterestModel { flat }

enum InstallmentMode { fixed, variable }

enum InstallmentStatus { unpaid, partial, paid, overdue }

enum BudgetPeriod { daily, monthly, weekly, yearly, once }

enum TransactionSortOrder { newest, oldest }
